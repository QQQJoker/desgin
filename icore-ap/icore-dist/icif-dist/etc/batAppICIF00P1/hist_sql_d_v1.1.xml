<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<sql id="dddRangePartition">
	   <![CDATA[
	      alter table ${main_table} add partition ${p_name} starting ('${tools.toDate2(p_start_time)}') ENDING('${tools.toDate2(p_end_time)}') in tp_data_big index in tp_data_index
	   ]]>
	</sql>
	 
	<sql id="detachPartitionData">
	   <![CDATA[
	   	    alter table ${main_table} detach partition ${p_name}  into ${main_table}_${p_name} ;
	   	    commit;
	   		alter table H_${main_table} attach partition ${p_name} starting (${lowval}) ENDING(${higval}) from  ${main_table}_${p_name};
	   		set integrity for H_${main_table}  IMMEDIATE  checked
	   ]]>
	</sql>
</sqls>